# httpd.conf — Configuración mínima para Apache en contenedor
ServerRoot "/usr/local/apache2"
Listen 80

# Módulos esenciales
LoadModule mpm_event_module     modules/mod_mpm_event.so
LoadModule authz_core_module    modules/mod_authz_core.so
LoadModule dir_module           modules/mod_dir.so
LoadModule mime_module          modules/mod_mime.so
LoadModule log_config_module    modules/mod_log_config.so
LoadModule unixd_module         modules/mod_unixd.so
LoadModule headers_module       modules/mod_headers.so
LoadModule alias_module         modules/mod_alias.so
LoadModule rewrite_module       modules/mod_rewrite.so

# Usuario/grupo del proceso
User  www-data
Group www-data

# Nombre del servidor
ServerName localhost

# Silenciar advertencia de FQDN
ServerAdmin webmaster@localhost

# Raíz del contenido (volumen compartido)
DocumentRoot "/usr/local/apache2/htdocs"

<Directory "/usr/local/apache2/htdocs">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
    DirectoryIndex index.html
</Directory>

# Cabecera personalizada para identificar el nodo en logs
Header always set X-Apache-Node "%{SERVER_NAME}e"

# Logs
ErrorLog  /proc/self/fd/2
LogLevel  warn
LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined
CustomLog /proc/self/fd/1 combined

# MIME types
TypesConfig conf/mime.types
